{
  "name": "@convect/protocol",
  "version": "0.1.0",
  "description": "Protocol definitions and serialization contracts for Convect",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@convect/core": "workspace:*"
  }
}
